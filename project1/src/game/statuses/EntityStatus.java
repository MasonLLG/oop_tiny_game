package game.statuses;

/**
 * Enum representing the status of any entity (actor, ground, item) in the game.
 * Only shared statuses are included here
 *
 * @author kellytran
 */
public enum EntityStatus {
    BLESSED_BY_GRACE,
    CURSED,
}
